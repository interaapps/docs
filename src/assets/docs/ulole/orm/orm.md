UloleORM is an Object-Relation-Mapper.

It maps sql data into usable objects so you never need to write a single sql line anymore.

## Install with uppm
```bash
uppm i uloleorm
```

## Install with composer
```bash
composer require interaapps/ulole-orm
```